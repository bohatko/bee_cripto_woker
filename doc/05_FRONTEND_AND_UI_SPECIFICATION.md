# СПЕЦИФИКАЦИЯ FRONTEND & UI/UX (NEXT.JS 15)
## Проект: Bee Crypto Worker

---

## 1. Концепция дизайна и UI-система

Интерфейс платформы проектируется по современным канонам **Web3 / Neo-Fintech**:
* **Цветовая палитра**: Глубокая темная тема (`#0B0E14`, `#111622`, `#1A2234`) с неоновыми акцентами:
  * **Brand Primary**: Янтарно-золотой / Медовый неоновый (`#F59E0B`, `#FBBF24`) — символизирует рабочую пчелу (Bee Worker) и золото.
  * **Success / Profit**: Изумрудный зеленый (`#10B981`, `#059669`).
  * **Danger / Stop / Loss**: Рубиновый красный (`#EF4444`, `#DC2626`).
  * **Muted / Text**: Серебристо-серый (`#94A3B8`, `#CBD5E1`).
* **Типографика**: Чистые шрифты `Inter` или `Geist Sans` для текста, моноширинный `Geist Mono` или `JetBrains Mono` для цен, объемов и PnL.
* **Компоненты**: `shadcn/ui` на базе Tailwind CSS, Radix UI Dialogs, Tooltips, Tabs, Dropdowns.

---

## 2. Структура маршрутов Next.js (App Router)

```
src/app/
├── layout.tsx                  # Корневой лейаут (LanguageProvider, метаданные, шрифты)
├── globals.css
├── (public)/
│   └── page.tsx                # Главный лендинг (Hero с 3D-пчелой, продукты, калькулятор, тарифы, FAQ)
├── (auth)/
│   ├── login/page.tsx          # Вход
│   └── register/page.tsx       # Регистрация (активация 7-дневного триала)
├── (dashboard)/
│   ├── layout.tsx              # Защищенный лейаут кабинета (Sidebar + Header)
│   ├── dashboard/page.tsx      # Главный экран: баланс, health, активные позиции
│   ├── signals/page.tsx        # Сигналы Dip-Buy: статус, готовность, репликация
│   ├── signals/backtest/page.tsx   # Master paper-бенчмарк сигналов
│   ├── settings/exchange/page.tsx  # Подключение API-ключей (Binance, OKX, Bybit)
│   ├── settings/profile/page.tsx   # Профиль, Telegram, язык
│   ├── history/page.tsx        # История сделок
│   ├── history/bot/page.tsx    # Бэктест парного трейдинга
│   └── billing/page.tsx        # Инвойсы, QR-код для оплаты, статус подписки
├── admin/page.tsx              # Скрытая панель администратора
└── api/                        # Route Handlers (серверный доступ к Supabase/воркеру)
    ├── engine-config/route.ts
    ├── admin/engine-config/route.ts
    ├── admin/pair-market-data/route.ts
    ├── exchange/validate|set-primary|sync-balances/route.ts
    └── settings/profile|telegram-test/route.ts
```

---

## 3. Детальная спецификация экранов

### 3.1. Главный Лендинг (`/`) — реализовано (2026-09-15)

**Бренд:** `Crypto B` (от «крипто пчела»). Слоган: EN *"While you sleep, the bee works for you."* / RU «Пока вы спите, пчёлка работает за вас.»

1. **Header**:
   * Логотип: пчела в янтарной плашке + вордмарк `CRYPTO B`.
   * Навигация (якоря): «Продукты», «Как это работает», «Калькулятор прибыли», «Тарифы», «Безопасность», «FAQ».
   * Кнопки: «Войти» (ghost), «7 дней бесплатно» (amber), переключатель языка EN/RU.
2. **Hero-секция с интерактивной пчелой** (`web/src/components/landing/BeeHeroScene.tsx` + `bee-scene.ts`):
   * Three.js point-cloud пчела (~17,5 тыс. частиц: тело с медовыми полосами, голова, глаза, усики, жало, два машущих крыла) + фоновая «дымка».
   * Курсор работает «факелом»: точки рядом с указателем подсвечиваются и увеличиваются (кастомный ShaderMaterial, `uPointer` / `uPointerStrength`).
   * Пчела излучает монеты-тикеры (BTC, ETH, XRP, SOL, BNB, USDT) через Sprite-пул; клик по hero даёт залп монет (`uBurst`).
   * Parallax-наклон пчелы и камеры за курсором, idle-покачивание, взмах крыльев.
   * Оптимизация: `three` грузится динамическим import (отдельный чанк, вне First Load JS), пауза рендера вне viewport и при скрытой вкладке, cap pixel ratio 1.75, `prefers-reduced-motion` → статичный кадр, отсутствие WebGL → CSS-фолбэк с эмодзи-пчелой и тикер-чипами.
   * Живая плашка статуса: *"N из M пар корзины выше EMA10"* (Realtime `pair_market_data`).
3. **Риббон «Поиск идеальных пар для парного трейдинга»** (EN: *Finding the Perfect Pairs for Pair Trading*): карточки пар из `pair_market_data` (`current_ratio`, `ema_10`, `is_in_trend`) через Supabase Realtime; подзаголовок описывает живые данные простыми словами, без названий индикаторов.
4. **Продукты** — только фактическое описание того, что уже работает, без маркетинговых обещаний доходности и без внутренних деталей реализации:
   * «Корзина парного трейдинга»: общая суть (заработок на расхождении двух монет, до 4 независимых слотов, свой стоп и трендовый выход в каждом слоте).
   * «Dip-Buy сигналы»: общая суть (покупка резкой просадки и продажа отскока, три монеты и три горизонта, фиксированная цель и жёсткий стоп в каждой сделке, алерт в Telegram, опциональная репликация на аккаунт).
   * В каждой карточке — информационный блок, который **объясняет суть продукта**, а не только показывает цифры:
     * Корзина: «Что такое парный трейдинг» — одновременная покупка одной монеты и продажа другой равным объёмом, результат зависит от взаимного движения цен, а не от направления рынка. Затем цифры исследования: in-sample (6 мес., бэктест текущего режима движка с комиссиями и проскальзыванием) **+48.6%**, out-of-sample **+22.1%**, макс. просадка OOS **53.1%**; оговорка про 81.7% прибыли от одной пары и рост просадки вне выборки.
     * Сигналы: «Что такое Dip-Buy сигналы» — отслеживание резкого падения и покупка паники в расчёте на отскок, каждый сигнал — отдельная короткая сделка со своей целью и стопом. Затем цель/риск на сделку: XRP `+4% / −30%`, ETH `+2% / −15%`, BTC `+3% / −12%`; оговорка, что стоп в разы дальше цели.
   * Заголовки-лейблы «Research backtest — not a promise» и «Target and risk per trade» удалены; честность обеспечивается текстами-пояснениями и оговорками (`pairCaveat`, `signalCaveat`), а не заголовком.
5. **«Как это работает»**: 4 шага (подключение биржи → включение движков → сопровождение позиций круглосуточно без компьютера и браузера → live-мониторинг). Без упоминания хостинга, вендоров и внутреннего стека (Railway, CCXT, Supabase и т.п.). Блок «Уже реализовано в платформе» (чипы поверхностей) удалён.
6. **Калькулятор прибыли** (заменяет прежний калькулятор комиссии): ползунок стартового баланса **$1,200 – $500,000** (логарифмическая шкала + пресеты $1.2K / $5K / $20K / $100K / $500K) и выбор горизонта 1 / 3 / 6 / 12 / 60 месяцев. Доходность масштабируется сложным процентом от документированной in-sample доходности бэктеста **+110% за 6 месяцев**, затем вычитается фиксированная плата $20/нед (без комиссии с прибыли). Вывод: валовая прибыль, фиксированная плата, эффективная доходность, чистая прибыль и итоговый баланс.
7. **Тарифы**: $20/нед, без performance fee, 7 дней бесплатно, список из 6 фактических фич.
8. **Безопасность API**: 3 блока — запрет вывода, whitelist статических IP `208.77.244.240` / `152.55.185.189` / `152.55.185.190`, шифрование AES-256-GCM.
9. **FAQ** (нативный `<details>`): кастодия средств, поддерживаемые биржи, поведение при неоплате, плечо/риск корзины, ручное управление позицией. Ниже — футер.

> **Важно:** все прежние цифры доходности ($1,367,000 / 8.7% DD / 62.3% winrate) удалены с лендинга согласно аудиту (см. [`doc/02_STRATEGY_AND_BACKTESTS.md`](02_STRATEGY_AND_BACKTESTS.md) §4.0). Лендинг не показывает историческую доходность и не описывает стратегию как гарантированно прибыльную.


---

### 3.2. Главный Дашборд пользователя (`/dashboard`)
* **Виджет баланса**:
  * Общий баланс (Total Equity) в USDT.
  * Свободная маржа (Available Margin).
  * Дневной / Недельный PnL (+X.XX% / +$YYY).
* **Монитор здоровья системы (System Health Bar)**:
  * Три индикатора с зелеными/желтыми статусами:
    1. `Торговое ядро: В сети (12ms)`
    2. `Биржа (OKX): Подключено`
    3. `Статус бота: Активен (Корзина 4 пар)`
* **Кнопки управления (с обязательными модалками подтверждения)**:
  * Кнопка `Старт / Пауза`: включает или приостанавливает работу робота.
  * Кнопка `Panic Close All`: экстренный сброс всех позиций по рынку.
* **Таблица активных парных позиций**:
  * Столбцы: 
    * Пара (`ZEC/AVAX`, `ENA/SUI`...).
    * Время в сделке (чч:мм).
    * Нога LONG (цена входа / текущая / PnL).
    * Нога SHORT (цена входа / текущая / PnL).
    * **Суммарный PnL связки ($ и %)** с цветовой шкалой прогресса до TP (+5.0%) и SL (-1.5%).
* **Уведомления Supabase Realtime**:
  * Всплывающие toast-уведомления при закрытии сделок: *«Связка ZEC/AVAX успешно закрыта по Take-Profit: +$145.20 (+5.1%)»*.

---

### 3.3. Экран подключения бирж (`/settings/exchange`)
* Табы выбора биржи: **Binance**, **OKX**, **Bybit**.
* Поле: `API Key` (masked input).
* Поле: `API Secret` (password input).
* Поле: `Passphrase` (только для OKX).
* **Блок «Белый список IP»**:
  * Плашка: `208.77.244.240`, `152.55.185.189`, `152.55.185.190` (статические IP Railway EU West HA) + кнопка «Копировать».
  * Пояснение: *«Укажите этот IP в настройках биржи для 100% защиты ваших средств»*.
* **Кнопка «Проверить и сохранить»**:
  * При нажатии отправляется запрос валидации.
  * Если у ключа включен вывод средств — выдается блокирующая ошибка: *«Ошибка безопасности: отключите галочку 'Enable Withdrawals' на бирже!»*.
  * Если ключи корректны — выдается модалка подтверждения с балансом биржи.

---

### 3.4. Экран Биллинга и Инвойсов (`/billing`)
* **Статус подписки**:
  * Бейдж: `Пробный период (осталось 4 дня)` или `Подписка активна (до 10 сентября)`.
* **Текущий активный инвойс**:
  * Номер инвойса: `INV-2026-09-001`.
  * Таймер обратного отсчета: `До заморозки осталось: 38ч 14м`.
  * Сумма: **$20.00** (фиксированная недельная плата, без комиссии с прибыли).
* **Модальное окно оплаты (QR-код)**:
  1. Выбор сети: `USDT TRC-20` или `USDT BEP-20`.
  2. Генерация большого четкого **QR-кода** адреса биржи сервиса.
  3. Текстовое поле с адресом кошелька и кнопкой копирования в один клик.
  4. Поле для ввода: `TxID (Хеш транзакции)`.
  5. Кнопка «Подтвердить отправку перевода».
  6. После отправки статус инвойса меняется на `На проверке у администратора`.

---

## 4. Спецификация модальных окон подтверждения (Confirmation Modals)

Для исключения случайных кликов все критические действия требуют подтверждения:

```
┌────────────────────────────────────────────────────────┐
│  ⚠️ Подтверждение запуска торгового робота             │
├────────────────────────────────────────────────────────┤
│  Вы собираетесь активировать автоторговлю по корзине   │
│  из 4 рыночно-нейтральных пар.                         │
│                                                        │
│  • Подключенная биржа: OKX (Баланс: $5,240.50 USDT)    │
│  • Рабочее плечо: 3x                                   │
│  • Защитный стоп: ATR-based (1.5 × ATR14, cap 10%)     │
│                                                        │
│  Робот начнет открывать зеркальные позиции при         │
│  появлении первого торгового сигнала.                  │
├────────────────────────────────────────────────────────┤
│  [Отмена]                       [Подтвердить запуск]   │
└────────────────────────────────────────────────────────┘
```

```
┌────────────────────────────────────────────────────────┐
│  🚨 ВНИМАНИЕ: ЭКСТРЕННОЕ ЗАКРЫТИЕ ВСЕХ ПОЗИЦИЙ        │
├────────────────────────────────────────────────────────┤
│  Это действие немедленно отправит рыночные ордера      │
│  на биржу и закроет ВСЕ 4 открытые связки.             │
│                                                        │
│  Текущий суммарный плавающий PnL: +$42.10 USDT.        │
│                                                        │
│  Для подтверждения введите слово «ЗАКРЫТЬ»:            │
│  [ ЗАКРЫТЬ                              ]              │
├────────────────────────────────────────────────────────┤
│  [Отмена]                       [Закрыть все позиции]  │
└────────────────────────────────────────────────────────┘
```

---

## 5. Панель Администратора (`/admin`)

### 5.1. Инвойсы
* Таблица всех выставленных инвойсов пользователей:
  * ID пользователя и Email.
  * Период и чистая реализованная прибыль за период.
  * Сумма инвойса ($).
  * Указанный пользователем TxID с прямой кликабельной ссылкой на Tronscan / BscScan.
  * Кнопки действий:
    * 🟢 **Подтвердить (Approve)**: продлевает подписку пользователя на 7 дней в базе Supabase, снимает заморозку, отправляет email.
    * 🔴 **Отклонить (Reject)**: возвращает инвойс в статус неоплаченного с указанием причины (неверный хеш, сумма меньше необходимой).

### 5.2. Pairs & Rotation (added 2026-09-07)
Вкладка **Pairs** на `/admin` (Realtime на `strategy_pairs` и `pair_selection_runs`):

1. **Current Active Basket** — до 4 активных пар: score, metrics (t-stat / corr / β-diff / funding), `activated_at`.
2. **Auto-Rotation** toggle → `ConfirmModal` → UPDATE `engine_settings.auto_rotation_enabled` + `audit_logs`.
3. **Run pair selection now** → `ConfirmModal` → INSERT `pair_selection_runs` (`trigger_source='admin'`, `status='pending'`) + audit; статус прогона обновляется live (pending → running → completed/failed).
4. **Run history** — последние N прогонов с expandable candidates/replacements JSON.

Фильтры пар на `/history` и `/history/bot` строятся динамически из фактических `pair_symbol` в данных (плюс активная корзина), без хардкода четырёх тикеров.
Дашборд `TradeReadinessMonitor` читает пары из `pair_market_data` (пишет воркер по union-скану).
